package newPackage;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.Select;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;

public class TestClass {

	public WebDriver driver;

	@BeforeMethod
	public void setup() {

		driver = new ChromeDriver();

	}
	
	@Test
	public void loadWeb() {
		
		driver.get("http://blazedemo.com/");
		
		//WebElement drpSource = driver.findElement(By.xpath("//*[@name='fromPort']"));
		//Select Source = new Select(drpSource);
		//Source.selectByVisibleText("San Diego");
		

	}
	
	@Test
	public void findFlight() {
		
		WebElement drpSource = driver.findElement(By.xpath("//*[@name='fromPort']"));
		Select Source = new Select(drpSource);
		Source.selectByVisibleText("San Diego");
		
		try {
			Thread.sleep(20000);
		} catch (InterruptedException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		//Select drpDestination = new Select(driver.findElement(By.name("toPort")));
		//drpSource.selectByVisibleText("Rome");
		
		//Select clickSubmit = new Select(driver.findElement(By.ByXPath);
	}
	
	
	@AfterMethod
	public void close() {
		driver.close();
	}
	

}
